<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_report"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_report"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaporanOps"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Teknisi -->
        <activity android:name=".ui.teknisi.TeknisiDashboardActivity"
            android:exported="false" />
        <activity android:name=".ui.teknisi.TambahLaporanActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Admin -->
        <activity android:name=".ui.admin.AdminDashboardActivity"
            android:exported="false" />
        <activity android:name=".ui.admin.DetailLaporanActivity"
            android:exported="false" />
        <activity android:name=".ui.admin.KelolaUserActivity"
            android:exported="false" />
        <activity android:name=".ui.admin.KelolaTowerActivity"
            android:exported="false" />

        <!-- Helpdesk -->
        <activity android:name=".ui.helpdesk.HelpdeskDashboardActivity"
            android:exported="false" />

        <!-- Shared -->
        <activity android:name=".ui.ProfileActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- FileProvider for camera/gallery -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
